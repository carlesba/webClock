# WebClock
	This HTML Watch shows a local TimeStamp. It's based in Javascript (jQuery), CSS and HTML and tested in Chrome 26, Firefox 19 and IE9.

# Structure
	- app.js:		It's the main program, it executes the oder parts of the app.
	-	model.js:	It controlls the -numbers- looking for changes in the timestamp.
	- view.js:	Responsible of the print function. Create the HTML elements and move them when they change.